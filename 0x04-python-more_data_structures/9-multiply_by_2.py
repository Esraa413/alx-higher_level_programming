#!/usr/bin/python3
def multiply_by_2(a_dictionary):
    dictionary = a_dictionary.copy()
    num_keys = list(dictionary.keys())

    for x in nuw_keys:
        dictionary[x] *= 2
    return (dictionary)
